-- poll plugin configuration

local config = {
    announce_time = 20, -- How many seconds between announcing there's a poll.
    point_bonus = 10 -- Award this many points to those participating in a poll. Requires the points plugin.
                     -- Set to 0 to disable.
}

return config