-- throttle plugin language strings

local lang = {
    too_fast = "You are sending too many messages. Wait %i seconds and try again.",
    command = "You are sending too many commands. Wait %i seconds and try again."
}

return lang